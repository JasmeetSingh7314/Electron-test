{
  "compilerOptions": {
    //require strict types(null-safety)
    "strict": true,

    //required to enable ecma-script
    "target": "ESNext",

    "module": "NodeNext",

    "outDir": "../../dist-electron",

    //ignore errors from libraries
    "skipLibCheck": true,
    "types": ["../../types"]
  }
}
